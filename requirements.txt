playwright>=1.44
beautifulsoup4>=4.12
lxml>=5.0
markdownify>=0.12
tenacity>=8.2
pyyaml>=6.0
# optional: accurate token counts for chunks; falls back to a heuristic if absent
tiktoken>=0.7
# dev/test only
pytest>=8.0
