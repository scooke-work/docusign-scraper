# Dependencies are declared in pyproject.toml. This file installs the package
# (and its runtime deps) so `pip install -r requirements.txt` keeps working.
#
#   Runtime only:        pip install -r requirements.txt
#   Dev + token counts:  pip install -e '.[dev,tokens]'
#   Then (required):     python -m playwright install chromium
#
# Or just run:  make setup
-e .
